luks_enable:  false
lvm_enable:   false
ext4_enable:  false
btrfs_enable: false
# btrfs_multi_devices: false    # if true for busybox hook, needed on host/group vars
swap_enable:  false

erase_disk: false         # if true then erase gpt.0.device

default_luks_name: luks
default_vg_name:   lvm

target_chroot: true       # true = on the mounted chroot /mnt of the target; false = on the target system directly
chroot: /mnt
