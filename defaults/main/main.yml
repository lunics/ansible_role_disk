luks_enable:  false
lvm_enable:   false
ext4_enable:  false
btrfs_enable: false
swap_enable:  false

default_luks_name: luks
default_vg_name:   lvm

target_chroot: true      # true = on the mounted chroot /mnt of the target; false = on the target system directly
chroot: /mnt

swapiness: 10       ## move to another role ?
